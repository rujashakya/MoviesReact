const initialData={
    username:'',
    email:''
}

const userReducer =(state= initialData,action) => {
    switch(action.type){
        default:
            return state
    }
}

export default userReducer